export enum apiMethods {
  showData = 'shows.GetById',
  showsList = 'shows.Get',
  showsNumber = 'shows.Count',
}

export enum queryKeys {
  pagination = 'page',
  search = 'search',
  pageSize = 'size',
  details = 'details'
}
